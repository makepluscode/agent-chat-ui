# Product Requirements Document (PRD)
# PDF RAG 챗봇 시스템

**버전**: 1.0
**작성일**: 2025-11-15
**상태**: Draft

---

## 1. 제품 개요

### 1.1 목적
사용자가 PDF 문서를 업로드하고, 업로드된 문서 내용에 대해 자연어로 질문하여 정확한 답변과 출처를 받을 수 있는 대화형 AI 시스템

### 1.2 목표
- PDF 문서를 쉽게 업로드하고 검색 가능하게 만들기
- 업로드된 문서 기반의 정확한 답변 제공
- 답변의 신뢰성을 위한 출처 정보 제공
- 실시간 스트리밍 응답으로 사용자 경험 향상

### 1.3 대상 사용자
- 대량의 PDF 문서를 다루는 연구자, 학생
- 법률, 의료 등 전문 문서 검토가 필요한 전문가
- 사내 문서 관리 및 검색이 필요한 기업

---

## 2. 기술 스택

### 2.1 프론트엔드
- **프레임워크**: Next.js 15.5 (React 19)
- **UI 라이브러리**: Agent Chat UI (LangChain 공식)
- **스타일링**: Tailwind CSS
- **상태 관리**: React Context (Stream, Thread providers)

### 2.2 백엔드
- **프레임워크**: LangGraph (Python)
- **LLM**: Ollama (Gemma 12B)
- **임베딩**: BGE-M3 (BAAI, 다국어 지원)
- **벡터 DB**: ChromaDB
- **PDF 처리**: PyPDF

### 2.3 인프라
- **로컬 개발**: LangGraph Dev Server (포트 2024)
- **벡터 저장소**: 로컬 파일 시스템 (./chroma_db)
- **파일 저장**: 로컬 디렉토리 (./uploads)

---

## 3. 핵심 기능 요구사항

### 3.1 PDF 업로드 및 처리

#### 3.1.1 기능 설명
사용자가 채팅 인터페이스에서 PDF 파일을 업로드하면 자동으로 처리

#### 3.1.2 세부 요구사항
- **입력**: PDF 파일 (최대 크기: 50MB)
- **처리 과정**:
  1. PDF 텍스트 추출 (PyPDF 사용)
  2. 텍스트 청킹 (RecursiveCharacterTextSplitter)
     - 청크 크기: 1000자
     - 오버랩: 200자
     - 구분자: 문단, 문장, 공백 순
  3. 임베딩 생성 (BGE-M3)
  4. 벡터 DB 저장 (ChromaDB)

#### 3.1.3 결과 표시
업로드 완료 후 다음 정보를 사용자에게 표시:
```
✅ PDF 업로드 성공

📄 파일 정보
- 파일명: [filename]
- 총 페이지: [total_pages]
- 생성된 청크: [total_chunks]

🔍 벡터 저장 정보
- 임베딩 모델: BAAI/bge-m3
- 저장된 청크: [stored_chunks]
- 컬렉션: pdf_documents
- 청크 크기: 1000 (overlap: 200)
- 평균 청크 길이: [avg_length] 문자

이제 이 문서에 대해 질문할 수 있습니다!
```

#### 3.1.4 에러 처리
- 지원하지 않는 파일 형식: "PDF 파일만 업로드 가능합니다"
- 파일 크기 초과: "파일 크기는 50MB 이하여야 합니다"
- 텍스트 추출 실패: "PDF에서 텍스트를 추출할 수 없습니다 (이미지 기반 PDF일 수 있습니다)"
- 저장 실패: "벡터 DB 저장 중 오류가 발생했습니다"

### 3.2 RAG 기반 질의응답

#### 3.2.1 기능 설명
사용자 질문에 대해 벡터 검색을 수행하고 관련 문서를 참조하여 답변

#### 3.2.2 검색 프로세스
1. 사용자 질문 임베딩 생성
2. 벡터 DB 유사도 검색 (top-k=5)
3. 검색된 청크를 컨텍스트로 LLM에 전달
4. LLM 답변 생성 (스트리밍)
5. 출처 정보와 함께 표시

#### 3.2.3 답변 형식
```
[LLM 생성 답변]

---

📚 참조된 정보:

1. [filename] (페이지 [page])
- 유사도: [similarity]%
- 내용: [content_preview]...

2. [filename] (페이지 [page])
- 유사도: [similarity]%
- 내용: [content_preview]...

...
```

#### 3.2.4 답변 품질 기준
- **정확성**: 제공된 문서 내용 기반 답변
- **투명성**: 출처 명시 (파일명, 페이지, 유사도)
- **정직성**: 문서에 없는 정보는 "제공된 문서에서 해당 정보를 찾을 수 없습니다"라고 답변
- **가독성**: 마크다운 형식 지원

### 3.3 일반 대화

#### 3.3.1 기능 설명
문서 검색이 필요하지 않은 일반적인 질문에 대해 LLM이 직접 답변

#### 3.3.2 라우팅 로직
- PDF 업로드 감지 → PDF 처리 노드
- 검색 관련 키워드 ("검색", "찾아", "알려", "search") → RAG 노드
- 그 외 → 일반 대화 노드

### 3.4 대화 히스토리 관리

#### 3.4.1 기능 설명
각 스레드(thread)별로 대화 내역 저장 및 복원

#### 3.4.2 요구사항
- Thread ID 기반 대화 세션 관리
- URL 쿼리 파라미터로 threadId 저장
- 새 대화 시작 시 새 thread 생성
- 이전 대화 불러오기 지원

### 3.5 실시간 스트리밍

#### 3.5.1 기능 설명
LLM 응답을 실시간으로 스트리밍하여 사용자 대기 시간 체감 감소

#### 3.5.2 요구사항
- `on_chat_model_stream` 이벤트 지원
- 토큰 단위 스트리밍
- 스트리밍 중 UI 인디케이터 표시

---

## 4. 사용자 인터페이스

### 4.1 메인 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│ [☰] Agent Chat                        [새 대화] [설정] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  👤 안녕하세요!                                         │
│                                                         │
│     🤖 안녕하세요! PDF 문서를 업로드하거나             │
│        질문해주세요.                                    │
│                                                         │
│  👤 [📎 document.pdf]                                   │
│                                                         │
│     🤖 ✅ PDF 업로드 성공                               │
│        📄 파일 정보...                                  │
│                                                         │
│  👤 이 문서의 주요 내용은?                             │
│                                                         │
│     🤖 이 문서는...                                     │
│        📚 참조된 정보: ...                              │
│                                                         │
├─────────────────────────────────────────────────────────┤
│ [📎] [메시지를 입력하세요...]              [전송 ➤]   │
└─────────────────────────────────────────────────────────┘
```

### 4.2 UI 컴포넌트

- **파일 업로드 버튼**: 클립 아이콘 (📎)
- **메시지 입력창**: 자동 높이 조절 textarea
- **스크롤 투 바텀**: 새 메시지 도착 시 하단 스크롤
- **로딩 인디케이터**: 스트리밍 중 표시
- **마크다운 렌더링**: 코드 블록, 리스트 등 지원

---

## 5. 비기능 요구사항

### 5.1 성능
- **PDF 처리 시간**: 10페이지 기준 5초 이내
- **검색 응답 시간**: 첫 토큰까지 1초 이내
- **동시 사용자**: 로컬 환경 기준 1-5명

### 5.2 확장성
- **벡터 DB**: ChromaDB → Qdrant/Weaviate 마이그레이션 가능
- **LLM**: Ollama → OpenAI/Claude API 교체 가능
- **배포**: LangGraph Cloud 배포 지원

### 5.3 보안
- **파일 검증**: PDF 파일 형식 엄격 검증
- **파일 크기 제한**: 50MB
- **API 키 관리**: 환경 변수, 서버 사이드만 접근

### 5.4 사용성
- **언어 지원**: 한국어, 영어 (BGE-M3 다국어 모델)
- **반응형 디자인**: 모바일, 태블릿, 데스크톱
- **다크 모드**: 지원

---

## 6. 시스템 아키텍처

### 6.1 전체 구조

```
┌─────────────────┐
│  Agent Chat UI  │ (Next.js, React)
│  (Frontend)     │
└────────┬────────┘
         │ HTTP/SSE
         ↓
┌─────────────────┐
│  LangGraph      │ (Python)
│  Server         │
└────────┬────────┘
         │
    ┌────┴────┬──────────┬───────────┐
    ↓         ↓          ↓           ↓
┌────────┐ ┌──────┐ ┌────────┐ ┌─────────┐
│ Ollama │ │ BGE  │ │Chroma  │ │ PDF     │
│ LLM    │ │Embed │ │VectorDB│ │ Storage │
└────────┘ └──────┘ └────────┘ └─────────┘
```

### 6.2 데이터 플로우

#### PDF 업로드
```
User → Frontend → LangGraph → PDF Parser
                      ↓
                  Text Splitter
                      ↓
                  BGE Embedding
                      ↓
                   ChromaDB
                      ↓
                Frontend (결과 표시)
```

#### RAG 질의
```
User Query → Frontend → LangGraph
                           ↓
                      Query Embedding
                           ↓
                      Vector Search
                           ↓
                     Context + Query → Ollama LLM
                           ↓
                   Streaming Response → Frontend
```

---

## 7. 개발 단계

### Phase 1: 기본 인프라 (1주)
- [ ] Next.js Agent Chat UI 설정
- [ ] LangGraph 프로젝트 구조 생성
- [ ] Ollama Gemma 12B 연동
- [ ] 기본 대화 기능 구현

### Phase 2: PDF 처리 (1주)
- [ ] PDF 업로드 UI 구현
- [ ] PyPDF 파싱 로직
- [ ] 텍스트 청킹 구현
- [ ] BGE 임베딩 설정
- [ ] ChromaDB 연동

### Phase 3: RAG 검색 (1주)
- [ ] 벡터 검색 구현
- [ ] RAG 프롬프트 엔지니어링
- [ ] 출처 정보 표시
- [ ] 검색 품질 개선 (리랭킹 등)

### Phase 4: 최적화 & 테스트 (1주)
- [ ] 스트리밍 성능 최적화
- [ ] 에러 핸들링 강화
- [ ] 사용자 테스트
- [ ] 문서화

---

## 8. 성공 지표

### 8.1 기능 완성도
- PDF 업로드 성공률 > 95%
- 답변 정확도 (출처 기반) > 90%
- 출처 표시율 100%

### 8.2 성능
- PDF 처리 시간 < 5초 (10페이지 기준)
- 첫 토큰 응답 시간 < 1초
- 시스템 안정성 > 99%

### 8.3 사용자 만족도
- 답변 유용성 평가 > 4.0/5.0
- UI 사용성 평가 > 4.0/5.0

---

## 9. 위험 요소 및 대응

### 9.1 기술적 위험

| 위험 | 영향 | 확률 | 대응 방안 |
|------|------|------|-----------|
| Gemma 12B 성능 부족 | 높음 | 중간 | 더 큰 모델로 교체 (Llama 70B 등) |
| PDF 텍스트 추출 실패 | 중간 | 높음 | OCR 기능 추가 (Tesseract) |
| 벡터 검색 정확도 낮음 | 높음 | 중간 | 하이브리드 검색 (BM25+Vector) 도입 |
| 메모리 부족 | 중간 | 낮음 | 배치 처리, 청크 크기 조정 |

### 9.2 운영 위험

| 위험 | 영향 | 확률 | 대응 방안 |
|------|------|------|-----------|
| 대용량 PDF 처리 지연 | 중간 | 높음 | 비동기 처리, 진행률 표시 |
| 동시 사용자 증가 | 높음 | 중간 | 큐 시스템, 부하 분산 |
| 스토리지 부족 | 중간 | 중간 | 자동 정리, 저장 기간 제한 |

---

## 10. 향후 개선 사항

### 10.1 단기 (1-3개월)
- 다중 PDF 동시 업로드
- PDF 문서 관리 (삭제, 목록 조회)
- 검색 필터링 (특정 문서만 검색)
- 답변 북마크 기능

### 10.2 중기 (3-6개월)
- OCR 지원 (이미지 기반 PDF)
- 표/차트 추출 및 분석
- 문서 비교 기능
- 다국어 문서 지원 강화

### 10.3 장기 (6-12개월)
- 멀티모달 지원 (이미지, 오디오)
- 협업 기능 (팀 워크스페이스)
- 클라우드 배포
- 엔터프라이즈 기능 (권한 관리, 감사 로그)

---

## 부록 A: 기술 스택 상세

| 카테고리 | 기술 | 버전 | 용도 |
|---------|------|------|------|
| Frontend | Next.js | 15.5 | React 프레임워크 |
| Frontend | React | 19.0 | UI 라이브러리 |
| Frontend | Tailwind CSS | 4.0 | 스타일링 |
| Backend | Python | 3.11+ | 백엔드 언어 |
| Backend | LangGraph | 1.0+ | AI 워크플로우 |
| Backend | LangChain | 1.0+ | LLM 체인 |
| LLM | Ollama | Latest | LLM 서빙 |
| LLM | Gemma | 12B | 언어 모델 |
| Embedding | BGE-M3 | Latest | 텍스트 임베딩 |
| Vector DB | ChromaDB | 0.5+ | 벡터 저장소 |
| PDF | PyPDF | 5.1+ | PDF 파싱 |

---

**문서 종료**
